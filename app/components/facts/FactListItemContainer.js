import { connect } from 'react-redux'
import FactListItem from './FactListItem.jsx'

const mapStateToProps = (state) => {
  return {}
}

const mapDispatchToProps = (dispatch) => {
  return {}
}

export default connect(
  mapStateToProps, mapDispatchToProps
)(FactListItem)
